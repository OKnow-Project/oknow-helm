apiVersion: v1
kind: Service
metadata:
  name: {{ include "oknow-helm.fullname" . }}
  labels:
    {{- include "oknow-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oknow-helm.selectorLabels" . | nindent 4 }}
